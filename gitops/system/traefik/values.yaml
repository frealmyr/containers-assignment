traefik:
  ingressRoute:
    dashboard:
      enabled: false
  podSecurityPolicy:
    enabled: false
  kubernetesCRD:
    enabled: false
  ports:
    websecure:
      redirectTo:
        port: web # TODO: disabled HTTPS for MVP
  providers:
    kubernetesIngress:
      ingressClass: traefik
