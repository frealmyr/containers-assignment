traefik:
  ingressRoute:
    dashboard:
      enabled: false
  podSecurityPolicy:
    enabled: false
  kubernetesCRD:
    enabled: false
  ports:
    web:
      redirectTo:
        port: websecure
  providers:
    kubernetesIngress:
      ingressClass: traefik
